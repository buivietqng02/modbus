doctype html
html
  head
    title= title
    
    script(src="https://cdn.jsdelivr.net/npm/chart.js@3.6.0/dist/chart.min.js")
    link(rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous")
    link(rel= "shorcut icon" href="#")
    link(rel="stylesheet" href= "../stylesheets/style1.css")
    
      

     
      
    
  body
    
    div(class= "container-fluid")
        div(class="row display")
            div(class= "col-sm-2 menu" style= "border-right: 1px solid") 
                p Wellcome #{email}
                
                button(class= "btn-primary" id= "chart") Chart 
                button( class= "btn-primary" id="bill") View Bill 
            div(class= "col-sm-10")
                div(class="display-section")
                    h3 Chart 
                    h5(id="current") Power now
                    div(style= "display:flex;flex-direction: row ")
                
            
                        div(class= "graph per each hour" style="flex: 1; ")
                            h4 Chart for power (kW) in day
                            canvas(id="chart1")
                            label(for="select day") Select day
                            input(type= "date" name= "select day" )
                        div(class="graph accumulated day" style= "flex: 1")
                            h4 Chart for power comsumption each day in month
                            canvas(id="chart2")
                            label(for="select day") Select month 
                            input(type= "month"  )
                div(class="bill-section")


    script(). 
        

        var id=!{JSON.stringify(id)}
        console.log(id)
    script(src= '../javascripts/file1.js')

